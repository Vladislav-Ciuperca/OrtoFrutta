{
  "name": "esercitazione",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@cap-js/hana": "^1",
    "@sap/cds": "^8.9.4",
    "@sap/xssec": "^4",
    "cds": "^0.2.0",
    "dotenv": "^16.5.0",
    "express": "^4",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@cap-js/cds-types": "^0.10.0",
    "@cap-js/sqlite": "^1",
    "@sap/cds-dk": "^8",
    "mbt": "^1.2.29",
    "rimraf": "^5.0.5"
  },
  "scripts": {
    "start": "cds-serve",
    "start1": "cds watch --profile hybrid",
    "hybrid": "cds watch --profile hybrid",
    "watch-gestionale_reparto_frutta_coop": "cds watch --open gestionale_reparto_frutta_coop/webapp/index.html?sap-ui-xx-viewCache=false",
    "undeploy": "cf undeploy esercitazione --delete-services --delete-service-keys --delete-service-brokers",
    "build": "rimraf resources mta_archives && mbt build --mtar archive",
    "deploy": "cf deploy mta_archives/archive.mtar --retries 1"
  },
  "cds": {
    "requires": {
      "[production]": {
        "auth": "xsuaa",
        "db": "hana"
      },
      "auth": "xsuaa"
    },
    "sql": {
      "native_hana_associations": false
    },
    "fiori": {
      "preview": true
    },
    "server": {
      "index": true
    }
  }
}
